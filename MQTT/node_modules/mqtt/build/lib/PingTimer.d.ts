import type { TimerVariant } from 'mqtt/lib/shared';
export default class PingTimer {
    private keepalive;
    private timerId;
    private timer;
    private checkPing;
    constructor(keepalive: number, checkPing: () => void, variant: TimerVariant);
    clear(): void;
    reschedule(): void;
}
